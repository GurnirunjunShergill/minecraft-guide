// app/(site)/home/page.tsx
export default function SiteHomePage() {
  return (
    <section>
      <h1>Site Home</h1>
      <p>
        This page is under the <code>(site)</code> group, so it renders inside
        the navbar layout.
      </p>
    </section>
  );
}
